[[LinearDiscriminantAnalysis|LDA]] assumes that the observations within each class are drawn from a multivariate Gaussian distribution with a class specific mean vector and a covariance matrix that is commos to all $K$ classes.


#ISLP #statistics #probability #classification